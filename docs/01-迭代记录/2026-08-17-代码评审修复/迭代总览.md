# 迭代总览：代码评审修复

> 状态：已完成
> 日期：2026-08-17

## 背景

对项目全部受控源码（`lib\`、`scripts\`、`test\`、`.github\workflows\publish.yml`、`cordis.patch.yml`）做了 8 角度全量代码评审，并对照 `dsh-workspace`、`dsh-session-projection-cache` 等上游真实实现交叉验证。共确认 10 项发现，本迭代修复其中 9 项代码 / 流水线 / 文档问题（第 10 项即本迭代产出的文档体系本身）。

## 已完成内容

| 项 | 严重度 | 修复 |
|----|--------|------|
| #1 设置页项目筛选 value 与 options 脱钩 | HIGH | 选中分组消失时 effect 回退“所有项目”（`lib\client.js`） |
| #2 用 `workspace.title` 作分组 key / 筛选 value（重名冲突） | HIGH | 分组引入稳定 `key`（`workspaceId` / `__ungrouped__`），key 与 title 分离 |
| #3 父类 `coldSnapshot` 的 `putSoft` 绕过墓碑与 `whenIdle` | HIGH | `ArchiveProjectionCache` override `putSoft`：墓碑拦截 + 跟踪 + 落定后补删（`lib\projcache.js`） |
| #4 墓碑挡住同 id 冷复用（新生命周期永久 UNKNOWN） | HIGH | `deletedIdentities` 记录被删身份，`sessionKnown` 冷复用探针按身份差异撤墓碑（`lib\workspace.js`） |
| #5 “全部删除”漏掉摘要未加载的归档会话 | MEDIUM | 新纯函数 `deriveArchivedDeleteAllIds` 直接取归档全集，仅排除已知 subagent |
| #6 publish.yml 未声明 OIDC 依赖 | MEDIUM | 补充 Trusted Publishers 说明注释（npm 侧配置需管理员确认） |
| #7 FlatList 键名常量与字面量混用 | LOW | 统一用 `FLAT_SESSION_ORDER_KEY` |
| #8 墓碑淘汰算法两处重复 | LOW | 抽取 `lib\tombstone.js` 的 `trackTombstone` 共享 |
| #9 英文词典 “WorkSpace” 拼写 | LOW | 改为 “Workspace” |
| #10 英文注释 / 缺少交接文档 | 规范 | 被点名 JSDoc 译为中文；补建 `docs\00-交接入口` 与本迭代记录 |

## 验收结果

- `node --test test/*.test.mjs`：48 项全部通过（新增 8 项：设置页筛选断言 2 项、纯函数 3 项、宿主行为 3 项；按 clean cutover 删除客户端级联收集函数及其 2 项测试）。
- `node --check`：4 个 lib 文件语法通过。
- `pnpm build`（结构预检）通过。

## 复审跟进（同日）

复审确认 4 项低严重度发现，处理如下：

1. **死代码清理（已执行）**：`collectArchivedDeleteAllIds` / `collectSessionAndDescendantIds` 运行时已无调用点（删除全量与单删均走服务端级联），连同 `__test` 导出与测试一并删除，负向断言改为防回归守卫。
2. **cwd 归一（已执行）**：`headerIdentity` 把 cwd 缺失统一归一为 `null`，冷复用探针双侧经同一函数投影后比较，消除“一侧带键一侧不带键”的形状歧义；新增双向无 cwd / 单侧无 cwd 的边缘测试。
3. **deriveArchivedGroups O(n·m)（不动）**：设置页规模下可忽略，纯性能备注。
4. **trackTombstone set/order 脱同步边缘（不动）**：limit=4096 下不可达，注释已声明“宁多挡不错放”。

## 测试策略

- 宿主行为（`test\host.test.mjs`）：新增 putSoft 墓碑 / whenIdle 覆盖测试、冷复用探针测试（新生命周期放行 + stale 同生命周期仍未知）。
- 纯函数（`test\client.test.mjs`）：`deriveArchivedGroups`（重名工作区 key 唯一、空分组不产出）、`deriveArchivedDeleteAllIds`（摘要缺失仍入选）。
- 源码文本断言（`test\archive-settings-section.test.mjs`）：改写“全部删除”断言为归档全集语义，新增筛选 key 断言。

## 回滚方案

全部改动为源码级修改，单次提交完成；`git revert` 该提交即可整体回滚。无数据迁移、无配置变更。

## 关联提交

- 见本迭代对应提交（fix: 修复代码评审发现的设置页筛选 / 墓碑 / 冷复用等问题）。

## 阅读顺序

1. `docs\00-交接入口\项目概览.md`（了解三模块结构与关键设计）
2. 本文件
3. `lib\workspace.js` 的 `coldReuseKnown` / `lib\projcache.js` 的 `putSoft`（本次核心正确性修复）
4. `lib\client.js` 的 `deriveArchivedGroups` / `deriveArchivedDeleteAllIds`
