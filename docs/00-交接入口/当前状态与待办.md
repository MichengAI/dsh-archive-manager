# 当前状态与待办

> 更新时间：2026-08-17 14:30（Asia/Shanghai）

## 当前状态

- **阶段**：v0.1.10 之后、下一次发布之前的修复阶段。
- **代码评审**：已完成全量评审（10 项发现）+ 一轮复审（4 项低严重度跟进），全部处理完毕，见 [2026-08-17-代码评审修复](../01-迭代记录/2026-08-17-代码评审修复/迭代总览.md)。

## 当前事项

| # | 事项 | 状态 |
|---|------|------|
| 1 | 评审发现 #1-#5、#7-#10 修复 | 已完成 |
| 2 | 评审发现 #6（publish.yml OIDC） | 已完成（补充说明注释；npm 侧 Trusted Publisher 条目需仓库管理员在 npmjs.org 确认配置） |
| 3 | 发布 0.1.11（提交 + 打 tag 触发流水线） | 计划中（待负责人确认发布时机） |

## 交接风险

- **npm Trusted Publisher 配置**：若 npmjs.org 未为本仓库配置 OIDC Trusted Publisher，打 tag 后 `npm publish` 会失败；该配置无法通过仓库内代码验证，需管理员在 npm 侧核对。
- **测试依赖宿主环境**：`test/node_modules` 是指向 `%USERPROFILE%\.dsh\profiles\node_modules` 的 junction，本机外（CI）需自备同版本 `@deepseek-ai/*` 依赖。

## 待办（后续建议）

- 设置页筛选 / 分组改造上线后，观察真实数据下“未分组”桶与重名工作区的表现。
- 若上游 `SessionProjectionCache` 改为多阶段异步写（`write` 一次性假设失效），`writeCore`/`putSoftCore` 的补删守卫需要重新评估。
